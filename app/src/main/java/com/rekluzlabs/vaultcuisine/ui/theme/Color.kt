package com.rekluzlabs.vaultcuisine.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand Accents ──
val FreshMint = Color(0xFF98D8C8)
val SoftCherry = Color(0xFFFFB7B2)

// ── Pantry (Light) ──
val PantryBackground = Color(0xFFF4F9F6)
val PantrySurface = Color(0xFFFFFFFF)
val PantryText = Color(0xFF1E2925)

// ── Cellar (Dark) ──
val CellarBackground = Color(0xFF161D1A)
val CellarSurface = Color(0xFF222C28)
val CellarText = Color(0xFFE2ECE8)

// ── Deep Vault (AMOLED) ──
val VaultBackground = Color(0xFF000000)
val VaultSurface = Color(0xFF121212)
val VaultText = Color(0xFFF0F4F2)

// ── Garden Fresh (Green) ──
val GardenBackground = Color(0xFFD1ECE4)
val GardenSurface = Color(0xFFE6F5F0)
val GardenText = Color(0xFF0F1A16)
